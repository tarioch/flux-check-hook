apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: demo
spec:
  interval: 1h
  chart:
    spec:
      chart: demo
      version: 1.2.3
      sourceRef:
        kind: HelmRepository
        name: charts
  values:
    ingress:
      enabled: true
